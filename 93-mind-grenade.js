const num1 = 5
const num2 = 10

function addValues() {
    console.log(`the sum is : ${num1 + num2}`);
}

// the function below executes when we "require" this program, even without calling it.
addValues()